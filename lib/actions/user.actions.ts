"use server";

export const signIn = async () => {
  try {
    // TODO: Mutation / Database / Make fetch
  } catch (error) {
    console.error("Error", error);
  }
};

export const signUp = async (userData: SignUpParams) => {
  try {
    // TODO: Create a user accoutn
  } catch (error) {
    console.error("Error", error);
  }
};
